# Core — scraper
requests==2.32.*
mwparserfromhell==0.6.*
jsonlines==4.*
requests-cache==1.2.*

# CLI (modo --cli)
typer>=0.13

# GUI
PySide6>=6.7

# Language detection (opcional — activa scraper/lang_filter.py)
langdetect>=1.0.9

# NLP — Name Tagger (opcional — activa curator/name_tagger.py)
# Los modelos de idioma se descargan por separado desde la GUI (panel Idiomas)
# o con: python -m spacy download <model_name>
spacy>=3.7

# Tokenización exacta — Token Analyzer (OPCIONAL — no requerido para uso normal)
# El Token Analyzer usa un proxy de caracteres por defecto (~4 chars/token),
# suficientemente preciso para filtrar overlength en datasets de fine-tuning local.
#
# Si se requiere conteo exacto para LLaMA / Mistral / Qwen / Phi (modelos locales):
#   pip install transformers   (~500 MB — descarga tokenizador del modelo desde HuggingFace)
#
# tiktoken NO es compatible con LLaMA/Mistral/Qwen; solo sirve para modelos GPT de OpenAI.

# Dev / Testing
pytest==8.*
pytest-cov==5.*
